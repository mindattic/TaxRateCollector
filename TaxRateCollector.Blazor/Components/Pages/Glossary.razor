@page "/glossary"
@attribute [Authorize]
@rendermode InteractiveServer

<PageTitle>Glossary — TaxRateCollector</PageTitle>

<div class="page-header">
    <h1 class="page-title">Glossary</h1>
    <p class="page-sub">Definitions for every term, standard, and acronym used in this application.</p>
</div>

<div class="glossary-search-bar">
    <input class="setting-input" placeholder="Filter terms…"
           @bind="search" @bind:event="oninput" />
</div>

@foreach (var grp in Sections.Where(s => s.Terms.Any(t => Matches(t))))
{
    <div class="glossary-section">
        <h2 class="glossary-section-title">@grp.Title</h2>
        <dl class="glossary-list">
            @foreach (var term in grp.Terms.Where(t => Matches(t)))
            {
                <div class="glossary-entry" id="@Slug(term.Name)">
                    <dt class="glossary-term">
                        @term.Name
                        @if (!string.IsNullOrEmpty(term.Abbr))
                        {
                            <span class="glossary-abbr">(@term.Abbr)</span>
                        }
                        @if (!string.IsNullOrEmpty(term.Badge))
                        {
                            <span class="glossary-badge @term.BadgeClass">@term.Badge</span>
                        }
                    </dt>
                    <dd class="glossary-def">@((MarkupString)term.Html)</dd>
                </div>
            }
        </dl>
    </div>
}

@if (!Sections.Any(s => s.Terms.Any(t => Matches(t))))
{
    <div class="empty-state">No terms match "<strong>@search</strong>".</div>
}

@code {
    private string search = "";

    private bool Matches(GlossaryTerm t) =>
        string.IsNullOrEmpty(search) ||
        t.Name.Contains(search, StringComparison.OrdinalIgnoreCase) ||
        (t.Abbr?.Contains(search, StringComparison.OrdinalIgnoreCase) ?? false) ||
        t.Html.Contains(search, StringComparison.OrdinalIgnoreCase);

    private static string Slug(string name) =>
        name.ToLowerInvariant().Replace(" ", "-").Replace("/", "-").Replace("(", "").Replace(")", "");

    private record GlossaryTerm(string Name, string Html, string Abbr = "", string Badge = "", string BadgeClass = "");
    private record GlossarySection(string Title, List<GlossaryTerm> Terms);

    private static readonly List<GlossarySection> Sections =
    [
        new("The SST Initiative",
        [
            new("Streamlined Sales Tax",
                "A multi-state initiative launched in 2000 to simplify and modernize sales and use tax collection and administration. " +
                "Member states agree to adopt uniform definitions, simplified tax bases, and consistent administration rules so that " +
                "out-of-state sellers can more easily comply with sales tax laws in all participating states.",
                "SST"),

            new("Streamlined Sales Tax and Use Tax Agreement",
                "The binding legal document that governs SST membership. States that ratify the SSUTA agree to adopt its definitions, " +
                "exemptions, and simplified rules verbatim. <strong>Appendix C</strong> of the SSUTA contains the official product and service " +
                "taxonomy — the definitive source for how goods and services are classified for tax purposes in member states. " +
                "The agreement is amended periodically; the current version is managed by the SST Governing Board.",
                "SSUTA"),

            new("SST Governing Board",
                "The administrative body formed by SST member states. It maintains the SSUTA, publishes amendments, and operates " +
                "the Taxability Matrix portal (<em>sst.streamlinedsalestax.org/TM</em>) where each member state discloses how it " +
                "treats every product and service category defined in Appendix C.",
                "SSTGB"),

            new("SST Member State",
                "A state that has fully ratified the SSUTA and is bound by its uniform definitions. As of 2025 there are " +
                "<strong>24 full members</strong>: AR, GA, IN, IA, KS, KY, MI, MN, NE, NV, NJ, NC, ND, OH, OK, RI, SD, TN, UT, VT, WA, WV, WI, WY. " +
                "Local jurisdictions within a member state must follow the state's SST-derived taxability base — they cannot independently redefine exemptions. " +
                "Major states including CA, TX, NY, FL, IL, and PA are <em>not</em> members and define their own taxability rules independently.",
                Badge: "24 states", BadgeClass: "badge-sst"),

            new("Non-SST State",
                "A state with a sales tax that has <em>not</em> ratified the SSUTA. There are approximately 21 such states. " +
                "Their taxability rules for individual product/service categories can differ substantially from the SST definitions — " +
                "even when they use the same category name. The <strong>TaxCategory</strong> tree still applies as a classification scaffold, " +
                "but <strong>StateCategoryRule</strong> rows for these states must be sourced from each state's own statutes, not from the SST Taxability Matrix." +
                "<table class='gloss-table'>" +
                "<tr><th>State</th><th>Notable difference</th></tr>" +
                "<tr><td>California</td><td>Own definitions for candy &amp; dietary supplements; complex district taxes</td></tr>" +
                "<tr><td>Texas</td><td>Own definitions for food, software, and services; origin-based intrastate sourcing</td></tr>" +
                "<tr><td>Florida</td><td>Broad sales tax base with unique service exemptions; no personal income tax</td></tr>" +
                "<tr><td>New York</td><td>Clothing under $110 exempt; complex state + NYC + county layers</td></tr>" +
                "<tr><td>Illinois</td><td>Two-tier rates: 1% food/drugs, 6.25% general</td></tr>" +
                "<tr><td>Pennsylvania</td><td>Clothing and most food fully exempt; taxable items differ from SST</td></tr>" +
                "<tr><td>Arizona</td><td>Transaction privilege tax — taxed on seller's privilege, not the buyer's purchase</td></tr>" +
                "<tr><td>Louisiana</td><td>State + parish rates; unique food and drug definitions</td></tr>" +
                "<tr><td>Colorado</td><td>~70 home-rule cities each administer their own sales tax independently</td></tr>" +
                "<tr><td>Virginia</td><td>Reduced 2.5% food rate; definitions diverge from SST in several categories</td></tr>" +
                "</table>" +
                "<em>Five states levy no sales tax at all: AK, DE, MT, NH, OR.</em>",
                Badge: "~21 states", BadgeClass: "badge-warn"),

            new("SST Taxability Matrix",
                "A state-by-state disclosure table published at <em>sst.streamlinedsalestax.org/TM</em>. Each SST member state " +
                "declares whether each Appendix C product/service category is <em>Taxable</em>, <em>Exempt</em>, or subject to a " +
                "<em>Special Rule</em> in that state. This is the primary machine-queryable source for category-level taxability data."),

            new("SSUTA Appendix C",
                "The product and service taxonomy appendix to the SSUTA agreement. It defines the official categories and subcategories " +
                "used by all SST member states for classifying goods and services: Food &amp; Food Ingredients, Prepared Food, Candy, " +
                "Clothing, Drugs, Digital Goods, etc. This is the authoritative source for the <strong>TaxCategories</strong> table " +
                "in this application. The PDF is linked in <a href='/settings'>Settings → SST Source URLs</a>."),

            new("Multistate Tax Commission",
                "A separate intergovernmental body (not part of SST) that also works on multi-state tax uniformity, particularly for " +
                "income and corporate taxes. Relevant here primarily for context — the MTC and SSTGB overlap in some policy areas " +
                "but govern different tax types.",
                "MTC"),
        ]),

        new("Tax Fundamentals",
        [
            new("Sales Tax",
                "A consumption tax levied at the point of sale on the transfer of goods (and sometimes services) to the end consumer. " +
                "Collected by the seller and remitted to the state/local tax authority. 45 U.S. states plus D.C. impose a general sales tax; " +
                "Alaska, Delaware, Montana, New Hampshire, and Oregon do not (though some Alaskan municipalities have local sales taxes)."),

            new("Use Tax",
                "The companion tax to sales tax. When a buyer purchases goods without paying sales tax (e.g., from an out-of-state seller " +
                "that didn't collect it), the buyer is legally required to self-remit <em>use tax</em> at the same rate. Post-<em>Wayfair</em>, " +
                "most online sellers are required to collect and remit for all states where they exceed economic nexus thresholds, " +
                "reducing uncollected use tax significantly."),

            new("Excise Tax",
                "A tax on the manufacture, sale, or use of a specific product — typically motor fuel, tobacco, and alcohol. " +
                "Unlike sales tax, excise tax is usually applied upstream (at the manufacturer or distributor level) and is embedded " +
                "in the final retail price. Collected at a fixed point in the supply chain (manufacturer, importer, or distributor) " +
                "and stored using the <strong>RemittancePoint</strong> field on TaxRate rows."),

            new("Transaction Privilege Tax",
                "Arizona's version of sales tax. Despite the similar effect, it is technically a tax on the <em>seller's privilege</em> " +
                "of doing business, not on the buyer's purchase. Practically, sellers pass it on to buyers. Arizona TPT is origin-based " +
                "for intrastate sales and is administered by the Arizona Department of Revenue (ADOR) together with city taxes under " +
                "a single license — unusual compared to other states.",
                "TPT",
                Badge: "AZ only", BadgeClass: "badge-warn"),

            new("Nexus",
                "The legal connection between a seller and a state that creates an obligation to collect and remit sales tax in that state. " +
                "Two types: <strong>Physical nexus</strong> (store, warehouse, employee, or property in the state) and " +
                "<strong>Economic nexus</strong> (exceeding a dollar/transaction threshold — typically $100,000 in sales or 200 transactions — " +
                "with customers in that state)."),

            new("Economic Nexus",
                "Post-<em>South Dakota v. Wayfair</em> (2018) doctrine allowing states to require tax collection from out-of-state sellers " +
                "based purely on sales volume, without any physical presence. Most states set the threshold at $100,000 in annual sales " +
                "or 200 separate transactions into that state. <em>All</em> interstate remote sales use destination-based sourcing regardless " +
                "of the state's intrastate rule."),

            new("Marketplace Facilitator",
                "A platform (Amazon, eBay, Etsy, Walmart Marketplace, etc.) that facilitates third-party sales and is required by law " +
                "to collect and remit sales tax on those sales. Post-<em>Wayfair</em>, all 45 taxing states + D.C. have marketplace " +
                "facilitator laws. When a sale goes through a facilitator, the third-party seller has <em>no</em> collection obligation " +
                "for that transaction. Tracked via <code>RemittancePoint.MarketplaceFacilitator</code> on TaxRate rows."),

            new("Taxability",
                "Whether a particular product or service is subject to sales tax in a given jurisdiction. Not the rate — just the yes/no/reduced " +
                "question. A product can be <em>Taxable</em> (full rate applies), <em>Exempt</em> (no tax), <em>Reduced Rate</em> " +
                "(a special lower rate, e.g., Tennessee's 4% food rate), or <em>Special Rule</em> (depends on additional conditions like " +
                "preparation method or transaction context)."),

            new("Tangible Personal Property",
                "Physical, movable property — the classic taxable item in most states. Goods you can touch: furniture, electronics, clothing, " +
                "vehicles, equipment. The default rule in nearly all states is that TPP sales are taxable unless specifically exempted. " +
                "Contrasts with services and digital goods, which are taxable only if the state explicitly extends its tax to them.",
                "TPP"),

            new("Digital Goods",
                "Electronically delivered products: music, videos, ebooks, apps, software downloads, and online games. Taxability varies " +
                "dramatically by state. SST member states use the <em>Digital Goods</em> category in Appendix C. Non-SST states often " +
                "have inconsistent or silent statutes. Roughly 30 states now tax at least some digital goods, but the boundaries differ — " +
                "a streaming subscription taxable in one state may be exempt in the next."),

            new("Software as a Service",
                "Cloud-delivered software accessed via a browser or API — no local installation. Highly contested taxability. Some states " +
                "treat it as a taxable digital service or data processing; others exempt it as a non-tangible service. No consistent SST " +
                "definition exists. Because the 'product' is delivered at the server, sourcing rules also vary (some states use the " +
                "customer's primary-use location; others prorate across all users). Requires jurisdiction-level <strong>StateCategoryRule</strong> " +
                "rows with Special Rule or Exempt treatment for most states.",
                "SaaS"),

            new("Exemption Certificate",
                "A document a buyer provides to a seller to claim a sales tax exemption at the time of purchase. Common types: " +
                "resale (buyer will resell the goods), manufacturer (direct-use exemption), agricultural, governmental, and non-profit. " +
                "The seller must retain the certificate to document why tax was not collected; otherwise the seller is liable. " +
                "Multi-state sellers use the SST Uniform Exemption Certificate (SSTGB Form F0003) across all SST member states."),

            new("Resale Certificate",
                "A subset of exemption certificates used when the buyer is purchasing goods specifically for resale, not for personal " +
                "or business consumption. The buyer's sales tax registration number is typically required. If the goods are later used " +
                "personally (rather than resold), the buyer must self-remit use tax. Resale certificates must be renewed periodically " +
                "and validated for each vendor relationship."),

            new("Tax Holiday",
                "A temporary period (usually a weekend) when a state suspends sales tax on specific categories of goods — commonly " +
                "back-to-school clothing, school supplies, hurricane preparedness items, or energy-efficient appliances. The exemption " +
                "applies to qualifying items up to price thresholds that vary by state. Tax holidays appear as date-bounded rate overrides " +
                "in the scraper's output."),

            new("Destination-Based Sourcing",
                "The majority rule: sales tax is owed to the jurisdiction where the buyer <em>receives</em> the goods, not where the " +
                "seller is located. This is why a company in California selling to a customer in Texas owes Texas sales tax (once nexus is established), " +
                "not California tax. All interstate remote sales use destination sourcing post-<em>Wayfair</em>. " +
                "Stored as <code>SourcingRule.DestinationBased</code>."),

            new("Origin-Based Sourcing",
                "The minority rule used by approximately 12 states for <em>intrastate</em> sales: tax is owed based on where the " +
                "<em>seller</em> is located. States: AZ, IL, MS, MO, OH, PA, TN, TX, UT, VA, and CA (Modified). " +
                "The seller applies their own city/county rate rather than the buyer's. Does not apply to interstate remote sales. " +
                "Stored as <code>SourcingRule.OriginBased</code>."),

            new("Modified Sourcing",
                "A hybrid rule used primarily by <strong>California</strong>: state and county rate components are origin-sourced " +
                "(seller's location), while city and special district components are destination-sourced (buyer's location). " +
                "The result is that a CA intrastate sale has a split-sourcing calculation. Stored as <code>SourcingRule.Modified</code>."),

            new("General Rate",
                "The standard sales tax rate applied to fully taxable goods with no special category treatment. " +
                "The base rate from which exemptions and reduced rates are carved out. Stored on <strong>StateTaxProfile</strong> " +
                "as the state-level anchor; county and city rates layer on top."),

            new("Effective Rate",
                "The total rate a customer actually pays at a given location: <code>State Rate + County Rate + City Rate + Special District Rate</code>. " +
                "Varies by the exact delivery address. A purchase in Austin, TX at 8.25% is: 6.25% (TX state) + 2% (combined local). " +
                "Computed by summing all active ancestor <strong>TaxRate</strong> rows in the jurisdiction chain."),
        ]),

        new("Taxability Categories",
        [
            new("Food & Food Ingredients",
                "SSUTA Appendix C category. Defined as <em>substances sold for ingestion or chewing by humans, consumed for taste or nutritional value.</em> " +
                "Excludes alcoholic beverages, tobacco, candy, and prepared food. Most SST states exempt this category entirely; a few " +
                "(e.g., Tennessee, Mississippi) apply a reduced rate rather than full exemption. California taxes food ingredients at 0% " +
                "but has its own definitions that diverge from SST in edge cases."),

            new("Prepared Food",
                "SSUTA Appendix C category. Food sold in a heated state, sold with eating utensils provided by the seller, or two or more " +
                "food ingredients mixed or combined by the seller for sale as a single item. Generally taxable in all states — " +
                "the food-ingredient exemption does not extend to restaurant meals or deli items. The hot/cold distinction is a " +
                "frequent audit issue (e.g., a cold sandwich vs. a heated one)."),

            new("Candy",
                "SSUTA Appendix C category. Defined as a preparation of sugar, honey, or other natural or artificial sweeteners " +
                "combined with chocolate, fruits, nuts, or other ingredients — <em>where the product does not contain flour</em>. " +
                "This distinction matters: a Kit Kat bar (contains flour) is classified as food ingredients in many SST states " +
                "and is exempt, while a Milky Way (no flour) is candy and is taxable. States treat this differently — check the " +
                "SST Taxability Matrix for each member state."),

            new("Clothing",
                "SSUTA Appendix C category. Human wearing apparel for general use. Many SST states exempt general clothing. " +
                "New York exempts clothing items under $110 per item but taxes formal wear and items above the threshold. " +
                "Pennsylvania exempts most clothing. Sports equipment and protective gear are usually taxable even when general " +
                "clothing is exempt. Accessories (belts, handbags, jewelry) are typically taxable."),

            new("Drugs & Medical",
                "SSUTA Appendix C categories covering prescription drugs (universally exempt), over-the-counter drugs, dietary supplements, " +
                "and medical equipment. Prescription drugs are exempt in all states. OTC drugs are exempt in most SST states but " +
                "taxable in some non-SST states. Dietary supplements are a frequent gray area — the SST definition excludes them " +
                "from the drug exemption, but some states extend exemption anyway."),

            new("Motor Vehicles",
                "Generally subject to a separate registration/title fee or use tax administered by the DMV rather than standard " +
                "sales tax. Rates and structures differ substantially from general sales tax. Trades, dealer incentives, and " +
                "dealer-installed options all affect the taxable base. Not modeled as standard TaxRate rows."),

            new("Digital Goods (SST Category)",
                "SSUTA Appendix C categories for electronically transferred products: <em>Specified Digital Products</em> " +
                "(digital audio, digital audiovisual works, digital books) and <em>Computer Software</em> (delivered electronically). " +
                "SST member states must either tax or exempt these consistently per their Taxability Matrix disclosure. " +
                "Non-SST states' treatment is governed by their own statutes and is frequently undefined or contested."),

            new("Services",
                "The Services branch of the SSUTA Appendix C taxonomy. Unlike goods, services are taxable in a state <em>only</em> if " +
                "the state affirmatively extends its sales tax to that service category. No default taxability. Major service categories: " +
                "telecommunications, information services, data processing, repair services, personal services, and admission charges. " +
                "States vary enormously — New Mexico taxes almost all services; most states tax only a narrow list."),

            new("Telecommunications",
                "SSUTA Appendix C service category, subject to detailed federal and state rules under the Federal Telecommunications " +
                "Act. Mobile services (voice + data) are taxed under a parallel regime: federal USF, state/local telecom taxes, and " +
                "E911 fees — many of which fall outside standard sales tax. VOIP taxability is contested in several states. " +
                "Intrastate vs. interstate calls may be taxed differently. Generally sourced to the customer's primary location."),
        ]),

        new("Excise Taxes",
        [
            new("Motor Fuel Excise Tax",
                "A per-gallon tax on gasoline, diesel, and alternative fuels (CNG, LNG, hydrogen). Levied at the distributor level " +
                "and embedded in the pump price. Federal rate (2025): 18.4¢/gal gasoline, 24.4¢/gal diesel. State rates range from " +
                "~14¢ (Alaska) to ~66¢/gal (Pennsylvania). Many states index their rate to the CPI or average wholesale price, causing " +
                "annual or quarterly automatic adjustments — these are tracked using <code>RateAdjustmentFrequency.Annual</code> or " +
                "<code>Quarterly</code> on the TaxRate row."),

            new("Tobacco Excise Tax",
                "Per-pack tax on cigarettes and per-weight or per-unit taxes on cigars, snuff, pipe tobacco, and smokeless tobacco. " +
                "Federal cigarette rate: $1.0066/pack. State rates range from 17¢/pack (Missouri) to $4.35/pack (New York). " +
                "Cigars and other tobacco products (OTP) use percentage-of-wholesale pricing in most states — " +
                "see <strong>OTP</strong> and <strong>Percentage of Wholesale</strong>."),

            new("Other Tobacco Products",
                "The category covering all tobacco products <em>other than</em> cigarettes: cigars, pipe tobacco, snuff, chewing tobacco, " +
                "and newer products like heated tobacco. Tax rate basis is almost always <em>percentage of wholesale price</em> rather " +
                "than per-unit. Common state rates: RI 80% of wholesale, NY 75% of wholesale (cigars), CA 56.93% of wholesale. " +
                "Federal OTP rate: 52.75% of manufacturer's sale price for large cigars. Stored with <code>RateBasis.PercentageOfWholesale</code>.",
                "OTP"),

            new("Alcohol Excise Tax",
                "Federal and state per-unit taxes on beer, wine, and spirits. Federal rates (post-CBMA): beer $3.50–$18/barrel, " +
                "wine $1.07–$3.40/gallon, spirits $2.70–$13.50/proof gallon (tiered by volume and ownership). " +
                "States layer additional rates on top. Rate basis varies: beer uses <code>FlatPerVolume</code> (per barrel or per gallon), " +
                "spirits use <code>FlatPerProofGallon</code>, wine uses <code>FlatPerVolume</code>."),

            new("Alcohol By Volume",
                "The percentage of a beverage that is pure ethanol by volume. The standard measurement for classifying alcohol " +
                "into excise tax tiers — most state and federal schedules define rate breaks at 7%, 14%, 20%, and 24% ABV. " +
                "Stored on TaxRate as <code>MinAbv</code> and <code>MaxAbv</code> (decimal fractions, e.g., 0.20 = 20%). " +
                "A rate with <code>MinAbv = 0.20</code> and no <code>MaxAbv</code> applies to spirits at 20% ABV or higher. " +
                "Beer is typically under 7% ABV; table wine 7–14%; fortified wine 14–24%; spirits 20%+.",
                "ABV"),

            new("Proof Gallon",
                "The unit of measurement for distilled spirits taxation. One proof gallon = one liquid gallon of spirits at exactly " +
                "50% alcohol by volume (100 proof). A gallon of 80-proof whiskey = 0.8 proof gallons. Federal and most state spirits " +
                "excise taxes are expressed as dollars per proof gallon. " +
                "Stored with <code>RateBasis.FlatPerProofGallon</code>.",
                "PG"),

            new("Average Wholesale Price",
                "The reference price used to compute OTP and some fuel excise taxes expressed as a percentage-of-wholesale. " +
                "Determined by each state's revenue department, typically on a quarterly basis using survey data from distributors. " +
                "Rate basis = <code>PercentageOfWholesale</code>; the rate percentage is applied against the AWP at time of sale. " +
                "Because AWP fluctuates, OTP effective tax amounts change even when the statutory rate stays constant.",
                "AWP"),

            new("Three-Tier Distribution System",
                "The alcohol supply chain structure mandated by most states post-Prohibition: " +
                "<strong>Supplier/Brewer → Licensed Distributor → Licensed Retailer → Consumer.</strong> " +
                "Excise taxes are collected and remitted by the <em>distributor</em> tier, not the retailer. " +
                "Direct-to-consumer shipping (e.g., winery clubs) is handled differently and varies by state permit law. " +
                "Tracked via <code>RemittancePoint.Distributor</code>."),

            new("Craft Beverage Modernization Act",
                "Federal legislation (2017, made permanent 2020) that created tiered federal excise tax rates for small craft producers: " +
                "breweries producing under 2M barrels/year, wineries under 250,000 gallons, and small distilleries pay reduced federal rates. " +
                "Affects rate lookup for small producers — the federal rate row depends on total annual production volume.",
                "CBMA"),

            new("Environmental / Carbon Tax",
                "Taxes on carbon emissions, fuel combustion, or hazardous materials. California's cap-and-trade program " +
                "effectively creates a carbon allowance cost embedded in fuel prices. Some states levy per-barrel fees on petroleum " +
                "products for spill funds. Rate adjustment frequencies are often monthly or quarterly based on allowance auction prices. " +
                "Tracked with <code>RateAdjustmentFrequency.Monthly</code> or <code>Quarterly</code>."),
        ]),

        new("Rate Mechanics",
        [
            new("Sale Context",
                "The transaction setting in which a rate applies. Stored as the <code>SaleContext</code> enum on each TaxRate row. " +
                "<code>Any</code> — applies to all sales (the default for sales tax rates). " +
                "<code>OnPremise</code> — consumed at the venue (bars, restaurants). " +
                "<code>OffPremise</code> — retail take-home sale (liquor stores). " +
                "<code>Wholesale</code> — distributor-to-retailer transfer. " +
                "<code>DirectToConsumer</code> — winery/brewery ship-to-home (DTC permit). " +
                "Most relevant for alcohol excise taxes, which distinguish bar vs. retail rates.",
                "SaleContext"),

            new("Is Included in Price",
                "A boolean flag on TaxRate. When <code>true</code>, the tax has already been remitted upstream " +
                "(e.g., an alcohol excise paid by the distributor) and is embedded in the wholesale cost the retailer pays. " +
                "The retailer does <em>not</em> see a line-item excise; the cost is buried in the product price. " +
                "When <code>false</code> (the default), the tax is collected and remitted separately at the remittance point. " +
                "Affects downstream effective-rate calculations — embedded taxes should not be double-counted.",
                "IsIncludedInPrice"),

            new("Rate Basis",
                "Describes <em>how</em> a tax rate is applied to determine the tax amount due. " +
                "Stored as the <code>RateBasis</code> enum on each TaxRate row. " +
                "Sales tax is always <code>Percentage</code>; excise taxes use the full range of bases depending on the product.",
                "RateBasis"),

            new("Percentage",
                "Rate basis: a decimal fraction of the retail sale price. The most common basis — all sales tax rates use this. " +
                "Example: 6.5% → stored as 0.065. Tax = Rate × Sale Price.",
                "RateBasis.Percentage"),

            new("Flat Per Unit",
                "Rate basis: a fixed dollar amount per countable unit sold, regardless of retail price. " +
                "Example: $0.231 per cigarette pack, $0.05 per disposable bag. Tax = Rate × Quantity. " +
                "Used for tobacco, bag fees, and some beverage container deposits.",
                "RateBasis.FlatPerUnit"),

            new("Flat Per Volume",
                "Rate basis: a fixed dollar amount per volume unit (gallon, liter, barrel). " +
                "Example: $0.184/gallon (federal gasoline excise), $1.07/gallon (federal wine excise). " +
                "Tax = Rate × Volume. Used for motor fuel, beer, and wine excise taxes.",
                "RateBasis.FlatPerVolume"),

            new("Flat Per Weight",
                "Rate basis: a fixed dollar amount per weight unit (ounce, pound, gram). " +
                "Example: $1.20/oz of tobacco in some state OTP schedules. Tax = Rate × Weight.",
                "RateBasis.FlatPerWeight"),

            new("Flat Per Proof Gallon",
                "Rate basis: a fixed dollar amount per proof gallon of distilled spirits. " +
                "Example: federal spirits excise $13.50/proof gallon (over 100,000 PG/yr). " +
                "Tax = Rate × Proof Gallons. See <em>Proof Gallon</em>.",
                "RateBasis.FlatPerProofGallon"),

            new("Percentage of Wholesale",
                "Rate basis: a decimal fraction applied to the <em>wholesale</em> price, not the retail price. " +
                "Used almost exclusively for OTP (cigars, pipe tobacco, snuff). " +
                "Example: Rhode Island 80% of wholesale means if a cigar costs the distributor $1.00, the tax is $0.80. " +
                "Tax = Rate × Wholesale Cost.",
                "RateBasis.PercentageOfWholesale"),

            new("Remittance Point",
                "Where in the supply chain the tax is collected and remitted to the government. Sales tax is always remitted by the " +
                "<strong>Retailer</strong> (or <strong>MarketplaceFacilitator</strong>). Excise taxes on alcohol and tobacco are " +
                "remitted by the <strong>Distributor</strong> or <strong>Manufacturer</strong>/<strong>Importer</strong>. " +
                "Stored as <code>RemittancePoint</code> enum on TaxRate rows.",
                "RemittancePoint"),

            new("Sourcing Rule",
                "Determines which jurisdiction's rate applies to an intrastate transaction: " +
                "<code>DestinationBased</code> (buyer's location — default for 38+ states and all interstate sales), " +
                "<code>OriginBased</code> (seller's location — 12 states for intrastate), or " +
                "<code>Modified</code> (California split: state/county = origin, city/district = destination). " +
                "Stored as <code>SourcingRule</code> enum on <strong>StateTaxProfile</strong>.",
                "SourcingRule"),

            new("Rate Adjustment Frequency",
                "How often a tax rate changes automatically without new legislation. " +
                "<code>Static</code>: fixed until legislature acts (all sales tax rates; most excise). " +
                "<code>Annual</code>: recalculates once per year (NJ fuel tax Jan 1; IL fuel tax CPI Jul 1). " +
                "<code>Quarterly</code>: recalculates each quarter based on price index (VA motor fuel AWP). " +
                "<code>Monthly</code>: rare; used in some carbon allowance systems. " +
                "Non-static rates need more frequent scrape cycles.",
                "RateAdjustmentFrequency"),

            new("Indexed Rate",
                "A tax rate whose value is automatically adjusted by a government formula — typically tied to the CPI, an average " +
                "wholesale price, or a commodity index — without requiring new legislation. Motor fuel taxes in many states are indexed. " +
                "Indexed rates carry a non-<code>Static</code> <strong>RateAdjustmentFrequency</strong> and must be re-scraped at each " +
                "adjustment date to keep the TaxRate table current."),

            new("Rate Cap",
                "A state-imposed ceiling on the combined local sales tax rate that a county or city may levy. " +
                "Stored on <strong>StateTaxProfile</strong> as <code>LocalRateCap</code>. When set, the sum of all local tiers " +
                "cannot exceed this value, regardless of individual ordinances. Example: Tennessee caps the local rate at 2.75%."),
        ]),

        new("Compliance & Administration",
        [
            new("Filing Period",
                "The interval at which a seller must file a sales tax return and remit collected tax: monthly, quarterly, or annual. " +
                "States typically assign the period based on the seller's average monthly tax liability — high-volume sellers file monthly. " +
                "Filing deadlines are usually the 20th or last day of the month following the period end. Late filing triggers penalty and interest."),

            new("Remittance",
                "The act of paying collected sales or excise tax to the appropriate tax authority. Sellers collect tax from buyers, " +
                "hold it in trust, and then remit it to each state/locality by the filing deadline. In a three-tier excise system, " +
                "the distributor remits to the state; the retailer never touches the excise tax directly."),

            new("Voluntary Disclosure Agreement",
                "A program allowing businesses with undetected prior-period nexus exposure to come forward, pay back taxes, and " +
                "receive a limited lookback period and waiver of penalties. Most states offer VDAs through the Multistate Tax Commission " +
                "(MTC) Voluntary Disclosure Program or directly. Typically limits back-period exposure to 3–4 years rather than the " +
                "full statute of limitations.",
                "VDA"),

            new("Lookback Period",
                "The number of prior years a state can assess in an audit or VDA. Typically 3–4 years for sales tax. " +
                "Extended lookback (up to 6–8 years) applies in cases of fraud or substantial underreporting. " +
                "A VDA typically shortens the lookback to 3 years in exchange for voluntary compliance."),

            new("Statute of Limitations",
                "The maximum period after a return is filed during which a state can assess additional tax, penalties, or interest. " +
                "Typically 3 years for sales tax (from the return due date). Extended to 6 years if the taxpayer understated by more " +
                "than 25%; unlimited if no return was filed. Varies by state.",
                "SOL"),

            new("Audit",
                "A formal examination of a business's books and records by a state tax authority to verify that the correct amount " +
                "of sales tax was collected and remitted. Auditors request purchase invoices, resale certificates, exemption " +
                "certificates, and sales records. Common triggers: anomalous tax-to-revenue ratios, high exemption claims, nexus " +
                "from business registration data, or random selection."),

            new("Amnesty Program",
                "A time-limited state initiative offering penalty and/or interest waivers to businesses that voluntarily register " +
                "and pay past-due taxes during the program window. Similar to a VDA but announced publicly and available to all " +
                "qualifying businesses without case-by-case negotiation. States typically run amnesty programs every 5–10 years."),

            new("Penalty & Interest",
                "Failure to file or pay sales tax on time results in: <strong>Late-filing penalty</strong> (typically 5–25% of tax due), " +
                "<strong>Late-payment penalty</strong> (5–10%), and <strong>Interest</strong> (usually the federal short-term rate + 3%, " +
                "accruing daily). Some states also impose a <em>negligence</em> penalty (20–25%) for systematic non-compliance."),

            new("Certificate of Compliance",
                "An SST-specific designation. A <em>Certified Service Provider</em> (CSP) or <em>Certified Automated System</em> (CAS) " +
                "that passes SST testing receives a Certificate of Compliance, meaning the software/service is certified to calculate " +
                "sales tax correctly for SST member states. Sellers using a certified system receive full liability relief for errors " +
                "caused by SST-provided data.",
                "CSP / CAS"),

            new("Economic Nexus Threshold",
                "The dollar or transaction volume trigger that creates economic nexus in a state. The <em>Wayfair</em> standard: " +
                "$100,000 in annual sales <em>or</em> 200 separate transactions. Some states use $100,000 <em>and</em> 200 transactions " +
                "(more lenient), or have lower thresholds ($10,000 for ND). Most states measure the prior calendar year; some use " +
                "rolling 12 months. Exceeding the threshold triggers a collection obligation as of the following registration date."),
        ]),

        new("Jurisdictions & Geography",
        [
            new("Jurisdiction",
                "Any governmental entity with authority to levy a sales tax. In this application the hierarchy is: " +
                "<strong>Country → State → County → City</strong>. " +
                "Each level can add its own rate on top of all parent levels."),

            new("FIPS Code",
                "Federal Information Processing Standards geographic code. States are 2-digit (e.g., 48 = Texas), counties are 5-digit " +
                "(state + 3-digit county, e.g., 48201 = Harris County, TX), places (cities) are 7-digit. " +
                "Used as the unique key for each jurisdiction row.",
                "FIPS"),

            new("Special District",
                "A sub-county taxing authority created for a specific purpose: transit authorities (MTA, BART), stadium districts, " +
                "improvement districts, etc. Can levy fractional additional sales tax rates. Not modeled as a separate tier in this " +
                "application — the combined effective rate from the source document captures their contribution."),

            new("Home Rule",
                "Legal doctrine in some states (most notably Colorado) allowing municipalities to administer their own sales tax " +
                "independently of the state. Home-rule cities write their own taxability definitions, rates, exemptions, and filing " +
                "procedures. This makes Colorado the most complex state for sales tax compliance — each of ~70 home-rule cities " +
                "is effectively its own mini tax authority.",
                Badge: "CO model", BadgeClass: "badge-warn"),

            new("Piggyback Tax",
                "A local tax that uses exactly the same tax base as the state tax. The local jurisdiction just adds a rate percentage " +
                "on top; it cannot independently exempt or tax categories differently than the state. Common in most non-home-rule states."),

            new("SST Uniform",
                "The local authority model in SST member states. Local jurisdictions within an SST state must follow the state's " +
                "SSUTA-derived taxability definitions exactly — they cannot create independent exemptions or taxability rules. " +
                "This is a core SST membership requirement and what makes SST states simpler to manage."),

            new("Gazetteer",
                "The US Census Bureau's reference files listing all jurisdictions (counties, cities, places) with their names, " +
                "FIPS codes, land area, and coordinates. Published annually. The <strong>2025 Gazetteer</strong> is this application's " +
                "source for the Jurisdictions table. URLs are configurable in Settings."),

            new("ZCTA",
                "ZIP Code Tabulation Area — a Census Bureau construct that approximates USPS ZIP codes for statistical purposes. " +
                "Used in the crosswalk files to map ZIP codes to counties and cities for rate lookup.",
                "ZCTA"),

            new("Crosswalk File",
                "A Census Bureau tab-delimited file that maps ZCTAs to counties or places (cities). Two files are used: " +
                "<em>ZCTA→County</em> and <em>ZCTA→Place</em>. Together they let any 5-digit ZIP code resolve to its " +
                "state/county/city jurisdiction chain and therefore its effective tax rate."),
        ]),

        new("This Application",
        [
            new("StateTaxProfile",
                "A record describing the <em>structural</em> characteristics of a state's sales tax system — independent of whether " +
                "jurisdiction rows have been imported. Captures: general rate, SST membership, local authority type (SstUniform / Piggyback / " +
                "HomeRule / Independent), local rate cap, and the state revenue agency URL. " +
                "One row per state; StateCode is the unique key."),

            new("StateCategoryRule",
                "A state × SST category rule row. Stores whether a specific product/service category is Taxable, Exempt, ReducedRate, " +
                "or SpecialRule in a given state, plus the override rate if applicable (e.g., 0.04 for Tennessee's 4% food rate). " +
                "Nullable TaxCategoryId until the SST taxonomy is imported. Combined with the Jurisdiction hierarchy, these rows " +
                "drive the rate calculations."),

            new("TaxCategoryRule",
                "A jurisdiction-level override row (not state-profile level). Used for county or city exceptions where a local " +
                "jurisdiction has a different rule than the state — rare outside home-rule cities. " +
                "Foreign keys to both a Jurisdiction and a TaxCategory."),

            new("TaxCategory",
                "A node in the SST product/service taxonomy tree. Populated from SSUTA Appendix C. " +
                "Two top-level types: <em>Goods</em> and <em>Services</em>. Leaf nodes (IsLeaf = true) are directly assignable " +
                "to a TaxRate or transaction. The table is empty until the Appendix C PDF is parsed and imported."),

            new("TaxRate",
                "A scraped rate record for one jurisdiction at one point in time, optionally scoped to a TaxCategory. " +
                "Stores the rate for that jurisdiction tier only (not cumulative). The effective rate = sum of all active " +
                "ancestor TaxRate rows in the Country → State → County → City chain for the matching category. " +
                "Key fields: <code>Rate</code>, <code>RateBasis</code>, <code>TaxCategoryId</code>, <code>ProductCategory</code>, " +
                "<code>SaleContext</code>, <code>RemittancePoint</code>, <code>MinAbv</code>/<code>MaxAbv</code>, " +
                "<code>IsCurrent</code>, <code>AutoApprove</code>, <code>SourceConfidence</code>."),

            new("IsCurrent",
                "A boolean flag on <strong>TaxRate</strong>. Only the row with <code>IsCurrent = true</code> represents " +
                "the live rate for a given jurisdiction/category combination. When a scraper detects a rate change, " +
                "it sets the old row to <code>IsCurrent = false</code> and inserts a new row — keeping full history without " +
                "physical deletes. The Jurisdictions UI and all rate calculations filter to <code>IsCurrent = true</code> only."),

            new("AutoApprove",
                "A boolean flag on <strong>TaxRate</strong> that controls whether a newly scraped rate is immediately " +
                "visible in the Jurisdictions UI and used in calculations. When <code>true</code> (the default when " +
                "\"Auto Approve\" is enabled in Setup settings), the rate appears without any manual review step. " +
                "When <code>false</code> (\"Needs Review\" mode), rates are stored but hidden from the UI until an " +
                "administrator approves them. Useful when scraping sources with lower confidence."),

            new("ProductCategory",
                "An enum on <strong>TaxRate</strong> that identifies the excise product type: " +
                "<code>Beer</code>, <code>Wine</code>, <code>Spirits</code>, <code>Alcohol</code> (general), " +
                "<code>Tobacco</code>, <code>Cigarettes</code>, <code>Cannabis</code>, <code>Fuel</code>, " +
                "<code>Hotel</code>, <code>RentalCar</code>, <code>Gaming</code>, and others. " +
                "When <code>ProductCategory</code> is <em>non-null</em>, the rate is an excise / special tax and appears " +
                "in the <strong>Excise &amp; Special Rates</strong> panel (collapsed by default) rather than the main " +
                "sales-tax rates panel. When <em>null</em>, the rate is a general sales tax and follows normal " +
                "<code>TaxCategoryId</code> scoping.",
                "ProductCategory"),

            new("Excise &amp; Special Rates Panel",
                "A collapsible section within each jurisdiction row in the Jurisdictions UI. Shows all <strong>TaxRate</strong> " +
                "rows where <code>ProductCategory</code> is non-null — i.e., excise taxes, sin taxes, and special levies " +
                "that sit alongside (not inside) the general sales tax system. Examples: Illinois liquor gallonage tax, " +
                "Minnesota beer excise, Chicago on-premises liquor surcharge. The panel is hidden by default; " +
                "a badge (⚡) on the toggle shows the count of excise rates on record for that jurisdiction."),

            new("SourceConfidence",
                "An enum on <strong>TaxRate</strong> indicating how trustworthy the source document is. " +
                "<code>Official</code> (default) — fetched live from an official government URL. " +
                "<code>Archive</code> — live URL was unreachable; rate came from a Wayback Machine snapshot. " +
                "<code>ThirdParty</code> — sourced from an aggregator or secondary site, not a government page. " +
                "<code>Unverified</code> — origin unknown or not yet validated. " +
                "Displayed as a badge in the UI (yellow for Archive, orange for ThirdParty, red for Unverified). " +
                "Stored on both the TaxRate and its linked SourceDocument.",
                "SourceConfidence"),

            new("Wayback Machine Fallback",
                "A resilience mechanism in the bulk state scrapers. When a government URL returns a non-success status " +
                "or a 200 response that lacks expected content, the scraper queries the <em>Wayback Machine availability API</em> " +
                "(archive.org) to retrieve the most recent archived snapshot. If a snapshot is found, " +
                "the rate is parsed from the archived copy and stored with <code>SourceConfidence.Archive</code>. " +
                "Enabled or disabled per-scraper via the <em>Wayback Machine Fallback</em> setting in Setup. " +
                "Prevents scraper failures when government pages are temporarily down or have restructured."),

            new("Bulk State Scraper",
                "A scraper that fetches tax rates for an entire state — all tiers (state, county, city) — in a single pass " +
                "by downloading one or a few government source files. Implements <code>IStateBulkScraper</code>. " +
                "Contrasts with the per-jurisdiction <code>IScrapeStrategy</code> pattern (CSV / HTML / Excel / PDF) " +
                "which requires a separate source URL per jurisdiction. Current bulk scrapers: " +
                "<strong>WI</strong> (sales tax schedule), <strong>IL</strong> (IDOR excise + Cook County + Chicago), " +
                "<strong>MN</strong> (beer, wine, spirits excise + Minneapolis downtown liquor). " +
                "Each scraper declares its SST category name so rates are automatically scoped to the correct " +
                "<code>TaxCategoryId</code> when saved."),

            new("ScrapeRun",
                "A single end-to-end execution of the scraper pipeline. Records start time, completion time, total jurisdictions " +
                "processed, changes detected, and error count. Each TaxRate row references the ScrapeRun that produced it."),

            new("Evidence / Source Document",
                "A file captured as proof for a scraped tax rate — typically the PDF, Excel file, CSV, or HTML page from the " +
                "government source. Stored on the filesystem at <code>%APPDATA%\\MindAttic\\TaxRateCollector\\evidence\\</code>. " +
                "Each SourceDocument row holds metadata (URL, MIME type, content hash) and links back to the TaxRate it supports."),

            new("Discovery",
                "Phase 2 of the data pipeline. Recursively traverses the jurisdiction tree and attempts to locate the authoritative " +
                "tax rate source URL for each jurisdiction. Results are logged (Found / NotFound / Skipped / Error) but no rates are " +
                "written. The goal is to populate each Jurisdiction's SourceUrl so the Phase 1 scraper knows where to fetch."),

            new("Scrape",
                "Phase 1 of the data pipeline. For each jurisdiction with a configured SourceUrl, routes the request to the " +
                "appropriate IScrapeStrategy (CSV, HTML table, Excel, PDF, API) and writes the parsed rates as new TaxRate rows. " +
                "Detects changes against the previous run via the diff engine."),

            new("LocalAuthorityType",
                "An enum on <strong>StateTaxProfile</strong> describing how local jurisdictions relate to state taxability rules: " +
                "<code>SstUniform</code> — local rates piggyback, definitions locked to SSUTA; " +
                "<code>Piggyback</code> — local rates add on but may differ slightly; " +
                "<code>HomeRule</code> — local jurisdictions define and administer their own taxes (Colorado, Louisiana); " +
                "<code>Independent</code> — state has no centralized local tax system or the relationship is complex."),

            new("Diff Engine",
                "The internal service (<code>DiffEngine</code>) that compares a freshly scraped rate against the most recent stored " +
                "TaxRate for that jurisdiction and category. If the rate changed, a new TaxRate row is written with the current " +
                "<code>ScrapeRunId</code>; if unchanged, the existing row is left in place and no insert occurs. " +
                "This keeps a full history of rate changes without storing duplicate rows for stable jurisdictions."),

            new("IScrapeStrategy",
                "The interface implemented by each format-specific scraper: <code>CsvScraper</code>, <code>HtmlTableScraper</code>, " +
                "<code>ExcelScraper</code>, <code>PdfScraper</code>, and <code>ApiScraper</code>. " +
                "The <code>RecursiveRateScraper</code> selects the appropriate strategy based on the MIME type or file extension " +
                "of the source document. Strategies return a normalized list of rate rows that feed the diff engine."),

            new("ClaudeRateLawExtractor",
                "The AI extraction service that processes unstructured source documents (PDFs, HTML pages, Excel files) using Claude. " +
                "Given document content and a jurisdiction context, it returns structured rate data including amounts, RateBasis, " +
                "effective dates, and category scoping. The output feeds into the standard scraper pipeline and is stored as TaxRate rows " +
                "with the source document linked as evidence."),

            new("SourceUrl",
                "The URL of the authoritative government source for a jurisdiction's tax rate. Stored on the <strong>Jurisdiction</strong> " +
                "entity. Populated by the Discovery phase. The Scrape phase fetches this URL and routes it through the appropriate " +
                "IScrapeStrategy. A null SourceUrl means the jurisdiction has not yet been discovered and will be skipped during scraping."),

            new("Subscriber",
                "A paying user of the TaxRateCollector SaaS product. Subscribers select states and are billed by county count at " +
                "~$0.01/county/month. Access to rate data is restricted to the counties within their subscribed states. " +
                "Stored in the <strong>Subscribers</strong> and <strong>SubscriberStates</strong> tables; billing is handled via PayPal."),
        ]),
    ];
}
